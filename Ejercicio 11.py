# Lista original
lista_original = [1, 1, 2, 3, 4, 4, 5, 1]

# Eliminar duplicados usando un conjunto y convertir de nuevo a lista
lista_procesada = list(set(lista_original))

# Mostrar la lista procesada
print(lista_procesada)